# Netlify build configuration for the client SPA, kept in version control
# instead of only the dashboard. SPA routing is handled by client/public/_redirects.
[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
